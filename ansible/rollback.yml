- hosts: k8s
  connection: local
  tasks:
    - name: Rollback canary deployment safely
      shell: |
        if [ -f kubernetes/canary-deployment.yaml ]; then
          kubectl delete -f kubernetes/canary-deployment.yaml --ignore-not-found
        else
          echo "Canary deployment file not found, skipping rollback"
        fi
